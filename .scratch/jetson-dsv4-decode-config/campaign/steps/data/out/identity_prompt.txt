You are the operator of a four-node Jetson Orin cluster serving a mixture-of-experts model with tensor and expert parallelism. Explain, in a short structured answer, the exact sequence of steps that happens when one user request with a long reasoning-style prompt arrives: how the prefix is cached, how chunked prefill feeds the scheduler, how speculative decoding proposes and verifies draft tokens, how the accepted tokens are committed, and where the KV cache finally frees its blocks once the request completes. End with the two numbers from the startup log that bound how many long-context requests can run at once, and state why raising the KV cache ceiling requires re-running the out-of-memory smoke test rather than trusting the previous result.